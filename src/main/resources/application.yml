spring:
  application:
    name: phonebook

  datasource:
    url: jdbc:postgresql://postgres:5433/phonebook_clean_arquitecture
    username: postgres
    password: 123

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect